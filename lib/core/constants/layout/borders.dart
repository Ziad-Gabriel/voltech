import 'package:flutter/material.dart';

class Borders {
  static BorderRadius get mainRadius => BorderRadius.circular(16);
  static BorderRadius get secRadius => BorderRadius.circular(8);
  static BorderRadius get navBarRadius =>
      BorderRadius.vertical(top: Radius.circular(35));
}
